sql
update
update `테이블명` set `행`='값' where `행`='값';
조건절 없으면 해당 선택 테이블 값 전체 변경

딸깍 버전
-유저 계정 & 권한
계정 생성
좌측바 어드미니스트레이션 -> 유저 앤 프리빌리지 -> 로그인 -> 로그인 네임, 리밋 투 호스트 매칭 : localhost, 비번 입력하기
권한 부여
스키마 프리빌리지 -> 유저 선택 -> 애드 엔트리... -> 권한 부여할 스키마 선택 -> 아래에 있는 부여할 권한 체크 -> 어플라이

-원격 접속
공용 계정 만들기
create user remote@'%' indentified by '1234';
% = 시작 IP가 어디든 상관 없이
grant 부여할 권한 on 테이블.* to remote@'%';